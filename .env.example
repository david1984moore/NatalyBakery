# Database (Supabase)
# For Render/servers: Add ?connection_limit=1 to prevent pool exhaustion
# Use connection pooler (port 6543) for DATABASE_URL when available
DATABASE_URL="postgresql://user:password@host.supabase.co:5432/postgres"
# Direct connection for migrations (bypasses pooler). Use port 5432 for Supabase.
# Can be same as DATABASE_URL if not using connection pooler.
DIRECT_URL="postgresql://user:password@host.supabase.co:5432/postgres"

# Stripe Payment Configuration
# Set to "true" to enable Stripe payment at checkout. When false/unset, customers submit orders without payment (Nataly will arrange payment separately).
# Use order-only mode while waiting for Stripe account approval.
# NEXT_PUBLIC_ENABLE_STRIPE_PAYMENT="true"
# Development: use Test mode keys (pk_test_..., sk_test_...) and a test webhook secret.
# Production: use Live mode keys (pk_live_..., sk_live_...) and a LIVE webhook endpoint + secret.
# See STRIPE_LIVE_SETUP.md for switching to live payments.
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."

# Email Configuration (Gmail via Nodemailer)
GMAIL_USER="your-email@gmail.com"
GMAIL_APP_PASSWORD="xxxx xxxx xxxx xxxx"
EMAIL_FROM="your-email@gmail.com"
EMAIL_TO="your-email@gmail.com"

# Application URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Bakery timezone for delivery date validation - Eastern for Delaware
# If unset, defaults to America/New_York.
# BAKERY_TIMEZONE="America/New_York"
NODE_ENV="development"

# Admin (for order management and delivery confirmation)
ADMIN_PASSWORD="your-secure-admin-password"
